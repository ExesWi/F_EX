# Архитектура проекта F_EX Java IDE

## 📋 Содержание

1. [Обзор архитектуры](#обзор-архитектуры)
2. [Слои архитектуры](#слои-архитектуры)
3. [Presentation Layer (Слой представления)](#presentation-layer-слой-представления)
4. [Service Layer (Слой сервисов)](#service-layer-слой-сервисов)
5. [Data/Model Layer (Слой данных и моделей)](#datamodel-layer-слой-данных-и-моделей)
6. [Infrastructure Layer (Инфраструктурный слой)](#infrastructure-layer-инфраструктурный-слой)
7. [Диаграммы взаимодействия](#диаграммы-взаимодействия)
8. [Потоки данных](#потоки-данных)
9. [Принципы проектирования](#принципы-проектирования)

---

## Обзор архитектуры

F_EX Java IDE построена на основе **многослойной архитектуры (Layered Architecture)** с элементами **MVC (Model-View-Controller)** паттерна. Проект разделен на четкие слои с определенными ответственностями.

### Основные принципы:

- **Разделение ответственности (Separation of Concerns)** - каждый класс имеет одну четкую задачу
- **Слабая связанность (Loose Coupling)** - компоненты взаимодействуют через интерфейсы и абстракции
- **Высокая связность (High Cohesion)** - связанные функции группируются вместе
- **Dependency Injection** - зависимости передаются через конструкторы

### Общая структура:

```
┌─────────────────────────────────────────────────────────┐
│              Presentation Layer (UI)                     │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ IdeController│  │HelloApplication│ │  FXML Views │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│              Service Layer (Business Logic)             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │RefactorRename│  │  ExePackager │  │FileOperations│  │
│  │   Service    │  │              │  │   Service    │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│         Data/Model Layer (Data Management)              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ CodeIndexer  │  │SettingsManager│  │RecentFiles   │  │
│  │              │  │              │  │   Manager    │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│        Infrastructure Layer (External Systems)         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ProjectDetector│ │ProjectModel   │  │  DebugSession│  │
│  │              │  │  Resolver    │  │              │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────┘
```

---

## Слои архитектуры

### 1. Presentation Layer (Слой представления)

**Ответственность:** Управление пользовательским интерфейсом, обработка событий, взаимодействие с пользователем.

**Компоненты:**

#### `IdeController`
- **Роль:** Главный контроллер приложения (MVC Controller)
- **Ответственность:**
  - Обработка всех UI событий (кнопки, меню, горячие клавиши)
  - Управление вкладками редактора
  - Координация работы всех сервисов
  - Обновление UI в ответ на изменения
  - Управление жизненным циклом компонентов
- **Зависимости:**
  - Все сервисы (через композицию)
  - JavaFX компоненты (через FXML injection)
- **Ключевые методы:**
  - `initialize()` - инициализация UI
  - `onOpenFolder()`, `onSave()`, `onRunProject()` - обработчики действий
  - `openFileInEditor()` - открытие файлов
  - `scheduleAutoComplete()` - автодополнение
  - `scheduleDiagnostics()` - диагностика кода

#### `HelloApplication`
- **Роль:** Точка входа JavaFX приложения
- **Ответственность:**
  - Инициализация JavaFX приложения
  - Загрузка FXML разметки
  - Настройка сцены и окна
  - Обработка ошибок запуска
- **Зависимости:** Только JavaFX

#### `Launcher`
- **Роль:** Launcher для jpackage
- **Ответственность:**
  - Запуск JavaFX Application
  - Совместимость с jpackage

#### FXML Views (`ide-view.fxml`)
- **Роль:** Описание структуры UI
- **Ответственность:**
  - Определение компонентов интерфейса
  - Привязка к контроллеру
  - Структура меню, панелей, вкладок

---

### 2. Service Layer (Слой сервисов)

**Ответственность:** Бизнес-логика приложения, обработка данных, координация операций.

**Компоненты:**

#### `RefactorRenameService`
- **Роль:** Сервис рефакторинга (переименование)
- **Ответственность:**
  - Анализ кода для поиска символов
  - Планирование переименования (preview)
  - Применение изменений
  - Умное игнорирование строк и комментариев
- **Ключевые методы:**
  - `planRename()` - создание плана переименования
  - `applyPlan()` - применение изменений
  - `replaceIdentifiersOutsideStringsAndComments()` - умная замена
- **Особенности:**
  - Использует state machine для парсинга Java кода
  - Корректно обрабатывает escape-последовательности
  - Игнорирует строки, комментарии, литералы

#### `RefactorUndoManager`
- **Роль:** Менеджер отмены рефакторинга
- **Ответственность:**
  - Сохранение резервных копий файлов
  - Отмена последнего рефакторинга
  - Управление историей изменений
- **Ключевые методы:**
  - `rememberBackup()` - сохранение backup
  - `undo()` - отмена изменений
  - `canUndo()` - проверка возможности отмены

#### `FileOperationsService`
- **Роль:** Сервис файловых операций
- **Ответственность:**
  - Безопасное переименование файлов
  - Валидация операций
  - Обработка ошибок файловой системы
- **Ключевые методы:**
  - `renameFile()` - переименование файла

#### `ExePackager`
- **Роль:** Сервис упаковки в EXE
- **Ответственность:**
  - Сборка fat JAR (ShadowJar)
  - Определение main class
  - Вызов jpackage
  - Управление процессом сборки
- **Ключевые методы:**
  - `packageAsExe()` - основная логика упаковки
  - `buildFatJar()` - сборка fat JAR
  - `runJpackage()` - вызов jpackage
  - `findMainClass()` - определение main class

#### `DebugSession`
- **Роль:** Сессия отладки
- **Ответственность:**
  - Управление процессом jdb
  - Отправка команд отладчику
  - Парсинг ответов
  - Очередь запросов
- **Ключевые методы:**
  - `connectSocket()` - подключение к процессу
  - `send()` - отправка команды
  - `request()` - запрос с обработчиком ответа
  - `stop()` - остановка отладки
- **Особенности:**
  - Асинхронная обработка команд
  - Буферизация вывода
  - Определение промпта jdb

#### `DebugParsers`
- **Роль:** Парсеры вывода отладчика
- **Ответственность:**
  - Парсинг списка потоков
  - Парсинг стека вызовов
  - Парсинг локальных переменных
- **Внутренние классы:**
  - `ThreadItem` - информация о потоке
  - `FrameItem` - информация о фрейме стека
  - `VarItem` - информация о переменной

#### `JavaProjectRunner` / `IntelliJProjectRunner`
- **Роль:** Запуск проектов
- **Ответственность:**
  - Определение main class
  - Компиляция проекта
  - Запуск процесса
  - Управление вводом/выводом
- **Ключевые методы:**
  - `run()` - запуск проекта
  - `findMainClass()` - поиск main метода
  - `compileProject()` - компиляция

---

### 3. Data/Model Layer (Слой данных и моделей)

**Ответственность:** Управление данными, индексация, кэширование, персистентность.

**Компоненты:**

#### `CodeIndexer`
- **Роль:** Индексатор кода
- **Ответственность:**
  - Индексация всех Java файлов проекта
  - Построение индекса классов, интерфейсов, методов
  - Быстрый поиск по индексу
  - Обновление индекса при изменениях
- **Ключевые методы:**
  - `indexProject()` - полная индексация
  - `indexFile()` - индексация одного файла
  - `findClasses()` - поиск классов
  - `findMethods()` - поиск методов
- **Структура данных:**
  - `Map<String, List<CodeElement>>` - индекс по имени
  - `CodeElement` - класс, интерфейс, метод с информацией о местоположении

#### `SettingsManager`
- **Роль:** Менеджер настроек (Repository-подобный)
- **Ответственность:**
  - Загрузка настроек из файла
  - Сохранение настроек
  - Предоставление типизированного API
  - Управление настройками по умолчанию
- **Ключевые методы:**
  - `get()`, `set()` - строковые настройки
  - `getBoolean()`, `setBoolean()` - булевы настройки
  - `getInt()`, `setInt()` - числовые настройки
  - `loadSettings()` - загрузка
  - `saveSettings()` - сохранение
- **Хранение:**
  - Файл `.ide-settings.properties` в корне проекта или home директории

#### `RecentFilesManager`
- **Роль:** Менеджер недавних файлов
- **Ответственность:**
  - Отслеживание открытых файлов
  - Поддержание списка недавних файлов
  - Ограничение размера списка
- **Ключевые методы:**
  - `markOpened()` - отметка файла как открытого
  - `list()` - получение списка недавних файлов
- **Структура данных:**
  - `List<Path>` с ограничением размера (FIFO)

#### `ProjectModelResolver.ProjectModel`
- **Роль:** Модель проекта
- **Ответственность:**
  - Хранение информации о source roots
  - Хранение classpath
  - Предоставление данных для компиляции и диагностики
- **Структура:**
  ```java
  class ProjectModel {
      List<Path> sourceRoots;  // Корни исходного кода
      List<Path> classpath;    // Путь к классам и библиотекам
  }
  ```

---

### 4. Infrastructure Layer (Инфраструктурный слой)

**Ответственность:** Взаимодействие с внешними системами, определение типов проектов, работа с файловой системой.

**Компоненты:**

#### `ProjectDetector`
- **Роль:** Детектор типа проекта
- **Ответственность:**
  - Определение типа проекта (Gradle, Maven, Java, IntelliJ IDEA)
  - Поиск проектов в подпапках
  - Проверка наличия build-файлов
- **Ключевые методы:**
  - `detectProjectType()` - определение типа
  - `findGradleProject()` - поиск Gradle проекта
  - `findMavenProject()` - поиск Maven проекта
  - `findJavaProject()` - поиск Java проекта
  - `isIntelliJIDEAProject()` - проверка IntelliJ проекта
- **Типы проектов:**
  - `GRADLE` - проект с build.gradle или gradlew
  - `MAVEN` - проект с pom.xml
  - `JAVA` - простой Java проект
  - `INTELLIJ_IDEA` - проект IntelliJ IDEA
  - `UNKNOWN` - неизвестный тип

#### `ProjectModelResolver`
- **Роль:** Резолвер модели проекта
- **Ответственность:**
  - Определение source roots
  - Определение classpath
  - Выполнение команд build-систем
  - Парсинг вывода инструментов
- **Ключевые методы:**
  - `resolve()` - основная логика разрешения
  - `resolveGradle()` - для Gradle проектов
  - `resolveMaven()` - для Maven проектов
  - `resolvePlainJava()` - для простых Java проектов
  - `resolveIntelliJ()` - для IntelliJ проектов
- **Особенности:**
  - Выполняет внешние команды (gradlew, mvn)
  - Парсит XML/текстовый вывод
  - Кэширует результаты

#### File System Watcher
- **Роль:** Мониторинг файловой системы
- **Ответственность:**
  - Отслеживание изменений файлов
  - Автоматическое обновление дерева проекта
  - Регистрация директорий для отслеживания
- **Реализация:**
  - Использует `WatchService` из `java.nio.file`
  - Регистрирует все директории проекта рекурсивно
  - Debounce для оптимизации обновлений

---

## Диаграммы взаимодействия

### Сценарий: Открытие проекта

```
User → IdeController.onOpenFolder()
         │
         ├─→ ProjectDetector.detectProjectType()
         │   └─→ Returns: ProjectType
         │
         ├─→ setProjectRoot()
         │   │
         │   ├─→ ProjectModelResolver.resolve()
         │   │   ├─→ Executes: gradlew/mvn commands
         │   │   └─→ Returns: ProjectModel (sourceRoots, classpath)
         │   │
         │   ├─→ CodeIndexer.indexProject()
         │   │   ├─→ Scans all .java files
         │   │   └─→ Builds index (classes, methods)
         │   │
         │   ├─→ buildFileTreeRoot()
         │   │   └─→ Creates TreeView structure
         │   │
         │   └─→ startFileWatcher()
         │       └─→ Registers WatchService for all directories
         │
         └─→ Updates UI (projectTree, statusLabel)
```

### Сценарий: Переименование символа

```
User → IdeController.onRename()
         │
         ├─→ RefactorRenameService.planRename()
         │   ├─→ Scans files for identifier
         │   ├─→ Parses code (ignores strings/comments)
         │   └─→ Returns: RenamePlan (preview)
         │
         ├─→ confirmRenamePlan() [UI Dialog]
         │   └─→ User confirms
         │
         ├─→ RefactorUndoManager.rememberBackup()
         │   └─→ Saves file backups
         │
         ├─→ RefactorRenameService.applyPlan()
         │   └─→ Writes modified files
         │
         └─→ refreshTreeIfUnderRoot()
             └─→ Updates project tree
```

### Сценарий: Отладка проекта

```
User → IdeController.onDebugProject()
         │
         ├─→ JavaProjectRunner.run() [with JDWP]
         │   └─→ Starts process with: -agentlib:jdwp=...
         │
         ├─→ DebugSession.connectSocket()
         │   ├─→ Starts jdb process
         │   ├─→ Connects to: localhost:5005
         │   └─→ Sets ready = true
         │
         ├─→ applyBreakpointsToJdb()
         │   └─→ Sends: "stop at Class:line"
         │
         ├─→ refreshDebugAll()
         │   ├─→ DebugSession.request("threads")
         │   ├─→ DebugParsers.parseThreads()
         │   ├─→ DebugSession.request("where")
         │   ├─→ DebugParsers.parseWhere()
         │   ├─→ DebugSession.request("locals")
         │   └─→ DebugParsers.parseLocals()
         │
         └─→ Updates UI (debugThreadsList, debugStackList, debugVarsList)
```

### Сценарий: Автодополнение

```
User types → IdeController.scheduleAutoComplete()
                │
                ├─→ [After delay] showCompletion()
                │   │
                │   ├─→ CodeIndexer.findClasses(prefix)
                │   │   └─→ Returns: List<CodeElement>
                │   │
                │   ├─→ CodeIndexer.findMethods(prefix)
                │   │   └─→ Returns: List<CodeElement>
                │   │
                │   ├─→ extractWordsForCompletion()
                │   │   └─→ Extracts words from current file
                │   │
                │   └─→ Shows ContextMenu with suggestions
                │
                └─→ User selects → insertCompletion()
                    └─→ Inserts text into CodeArea
```

---

## Потоки данных

### Поток данных: Компиляция и диагностика

```
CodeArea (User edits)
    │
    ▼
IdeController.scheduleDiagnostics()
    │
    ├─→ [Debounce 500ms]
    │
    ▼
compileWithJavacAndParseProblems()
    │
    ├─→ ProjectModelResolver.ProjectModel
    │   ├─→ sourceRoots → -sourcepath
    │   └─→ classpath → -classpath
    │
    ├─→ ProcessBuilder("javac", ...)
    │
    ├─→ Parse javac output
    │   ├─→ Error: "error: ..."
    │   └─→ Warning: "warning: ..."
    │
    └─→ Update problemsByFile
        │
        ▼
updateProblemsPanel()
    │
    ├─→ problemsList.setItems()
    ├─→ errorCountLabel.setText()
    └─→ warningCountLabel.setText()
        │
        ▼
applyHighlighting()
    │
    └─→ computeHighlightingWithProblems()
        ├─→ computeHighlighting() [syntax]
        ├─→ buildLineOverlaySpans() [problems]
        └─→ mergeStyleSpans()
            │
            ▼
CodeArea.setStyleSpans() [Visual update]
```

### Поток данных: Индексация проекта

```
setProjectRoot()
    │
    ▼
CodeIndexer.indexProject()
    │
    ├─→ Files.walk(projectRoot)
    │   └─→ Filter: .java files, !hidden dirs
    │
    ├─→ For each file:
    │   │
    │   └─→ indexFile()
    │       ├─→ Read file content
    │       ├─→ Match patterns:
    │       │   ├─→ CLASS_PATTERN
    │       │   ├─→ INTERFACE_PATTERN
    │       │   └─→ METHOD_PATTERN
    │       │
    │       └─→ Add to index:
    │           └─→ index.put(name, CodeElement)
    │
    └─→ Index ready
        │
        ▼
Used by:
    ├─→ Autocomplete
    ├─→ Go to Class/Symbol
    └─→ Find Usages
```

---

## Принципы проектирования

### 1. Single Responsibility Principle (SRP)

Каждый класс имеет одну четкую ответственность:

- `IdeController` - только UI логика
- `RefactorRenameService` - только переименование
- `CodeIndexer` - только индексация
- `SettingsManager` - только управление настройками

### 2. Dependency Injection

Зависимости передаются через конструкторы:

```java
// Примеры:
private final RefactorRenameService renameService = new RefactorRenameService();
private final SettingsManager settingsManager = new SettingsManager(projectRoot);
private final ExePackager exePackager = new ExePackager(this::logToConsole, this::updateStatus);
```

### 3. Separation of Concerns

Четкое разделение слоев:

- **Presentation** - только UI
- **Service** - бизнес-логика
- **Data** - управление данными
- **Infrastructure** - внешние системы

### 4. Observer Pattern

Используется для реактивности:

- `WatchService` - наблюдение за файловой системой
- `Platform.runLater()` - обновление UI из фоновых потоков
- Event handlers в JavaFX

### 5. Strategy Pattern

Разные стратегии для разных типов проектов:

- `ProjectDetector` определяет тип
- `ProjectModelResolver` выбирает стратегию разрешения
- `JavaProjectRunner` vs `IntelliJProjectRunner`

### 6. Command Pattern

Команды отладчика:

- `DebugSession.send()` - отправка команд
- `DebugSession.request()` - команда с обработчиком
- Очередь команд в `DebugSession`

### 7. Factory Pattern

Создание компонентов:

- `buildFileTreeRoot()` - создание дерева
- `buildTreeItem()` - создание элементов дерева
- `createGutter()` - создание gutter с breakpoints

---

## Модульная структура

Проект использует **Java Platform Module System (JPMS)**:

### `module-info.java`

```java
module com.example.f_ex {
    requires javafx.controls;
    requires javafx.fxml;
    requires javafx.web;
    
    requires org.controlsfx.controls;
    requires org.fxmisc.richtext;
    // ... другие зависимости
    
    opens com.example.f_ex to javafx.fxml;
    exports com.example.f_ex;
}
```

### Преимущества модульности:

- Четкие границы модулей
- Контроль зависимостей
- Возможность создания runtime image через jlink
- Лучшая инкапсуляция

---

## Управление потоками выполнения

### Основные потоки:

1. **JavaFX Application Thread** - основной UI поток
   - Все обновления UI должны быть в этом потоке
   - Используется `Platform.runLater()` для обновлений из других потоков

2. **Фоновые потоки:**
   - `code-indexer` - индексация проекта
   - `project-model` - разрешение модели проекта
   - `gradle-runner` - выполнение Gradle команд
   - `jpackage-runner` - упаковка в EXE
   - `file-watcher` - мониторинг файловой системы
   - `diagnostics` - компиляция и диагностика

### Синхронизация:

- `ConcurrentHashMap` для потокобезопасных коллекций
- `volatile` для флагов состояния
- `Platform.runLater()` для обновления UI

---

## Расширяемость

### Добавление нового типа проекта:

1. Добавить тип в `ProjectDetector.ProjectType`
2. Реализовать логику определения в `ProjectDetector`
3. Добавить метод разрешения в `ProjectModelResolver`
4. Создать Runner класс (если нужен)

### Добавление нового сервиса:

1. Создать класс в пакете `com.example.f_ex`
2. Реализовать логику в отдельном классе
3. Инжектировать в `IdeController` через композицию
4. Использовать в обработчиках событий

### Добавление новой функции рефакторинга:

1. Расширить `RefactorRenameService` или создать новый сервис
2. Добавить метод планирования (preview)
3. Добавить метод применения
4. Интегрировать в `IdeController`

---

## Производительность

### Оптимизации:

1. **Debounce** для частых операций:
   - Автодополнение (300ms)
   - Диагностика (500ms)
   - Обновление дерева (300ms)

2. **Кэширование:**
   - Результаты поиска (`searchCache`)
   - Индекс кода (`CodeIndexer.index`)
   - Модель проекта (`projectModel`)

3. **Lazy Loading:**
   - Дерево проекта загружается по требованию
   - Вкладки открываются только при необходимости

4. **Асинхронность:**
   - Все тяжелые операции в фоновых потоках
   - UI остается отзывчивым

---

## Тестирование

### Рекомендации по тестированию:

1. **Unit тесты** для сервисов:
   - `RefactorRenameService` - тесты переименования
   - `CodeIndexer` - тесты индексации
   - `SettingsManager` - тесты сохранения/загрузки

2. **Integration тесты** для:
   - `ProjectDetector` - определение типов проектов
   - `ProjectModelResolver` - разрешение моделей
   - `DebugSession` - взаимодействие с jdb

3. **UI тесты** (если необходимо):
   - Тестирование основных сценариев использования
   - Проверка обработки ошибок

---

## Заключение

Архитектура F_EX Java IDE построена на принципах:

- ✅ **Многослойность** - четкое разделение ответственности
- ✅ **Модульность** - использование JPMS
- ✅ **Расширяемость** - легко добавлять новые функции
- ✅ **Производительность** - оптимизации и кэширование
- ✅ **Поддерживаемость** - понятная структура и разделение

Проект следует лучшим практикам Java разработки и может служить основой для дальнейшего развития IDE.

---

**Версия документа:** 1.0  
**Дата обновления:** 2026-01-20  
**Автор:** F_EX Development Team
